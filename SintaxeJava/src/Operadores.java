public class Operadores {

    public static void main(String[] args) {
        // Operadore de Atribuiçao
        int exemplo1 = 10; // Atribuição

        // Operadores Aritméticos
        int soma = 1 + 2; // Adição
        int subtracao = 1 - 2; // Subtração
        int multiplicacao = 1 * 2; // Multiplicação
        int divisao = 1 / 2; // Divisão
        int modolo = 1 % 2; // Resto da Divisão

        // + Openador unário de valor positivo
        // - Operador unário de valor negativo
        // ++ Operador unário de incremento de valro
        // -- Operador unário de decremento de valor
        // ! Operador unário lógico de negação

        // Operador de Ingualdade
        int num1 = 1;
        int num2 = 2;
        if (num1 == num2) {

        }

        // Operador de Diferença
        int num = 1;
        int nu = 2;
        if (num != nu) {

        }

        // Operadores relacionais
        int valorA = 1;
        int valorB = 2;

        if (valorA > valorB) {
            // maior
        }

        if (valorA >= valorB) {
            // maior ou igual
        }

        if (valorA < valorB) {
            // menor
        }

        if (valorA <= valorB) {
            // menor ou igual
        }

        // Operadores Lógicos
        boolean condicao1 = false;
        boolean condicao2 = true;

        if (condicao1 && condicao2) {
            // && é usado quando desejamos que as duas expressoes sejam verdadeiras
        }

        if (condicao1 || condicao2) {
            // || é usado quando desejamos que pelo menos uma das expressoes seja verdadeira
        }
    }
}
